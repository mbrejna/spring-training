spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    generate-ddl: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # springfox bug
keycloak:
  realm: cloud
  resource: cloud-service
  auth-server-url: http://localhost:8100/auth
  public-client: true
